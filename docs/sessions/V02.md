# V02 · Memoria y seguridad

## Encargo

- ID y título: V02 · Memoria y seguridad.
- Sesión padre, si existe: I00 · Coordinación e integración.
- Modelo exacto y esfuerzo: `gpt-5.6-sol` / `high`.
- Objetivo verificable: persistir el modelo canónico del MVP con aislamiento por usuario e implementar autenticación local, sesiones, CSRF, cifrado de credenciales y callbacks OAuth verificables.
- Hito: base segura de memoria para V03–V07.
- Dependencias integradas y commits: V01 integrado en `origin/main` mediante `48cd329e2b131389cd38942413d06f8d706b3517`; CI remoto confirmado `SUCCESS` por I00.
- Commit base de `main`: `48cd329e2b131389cd38942413d06f8d706b3517`.
- SHA verificado de `origin/main`: `48cd329e2b131389cd38942413d06f8d706b3517`.
- Rama exclusiva: `codex/v02-memory-security`.
- Rama remota/upstream: `origin/codex/v02-memory-security`.
- Worktree exclusivo: `/Users/estebanromero/Documents/ChatGPT/CrashMemory-worktrees/v02-memory-security`.
- Cupo global asignado (1–4): 3 de 4 (raíz=1, I00=2, V02=3).
- Archivos/paquetes propios: `packages/db`, `packages/canonical-model`, `packages/security`, rutas auth de `apps/api`, migraciones V02 y documentación V02.
- Contratos consumidos y producidos: consume `2026-09-20.v1`; produce esquema SQL y APIs internas de repositorio/seguridad compatibles, sin cambiar contratos compartidos.
- Migraciones previstas y orden reservado: `0001_v02_memory_security.sql`, primera migración de aplicación tras V01 (que no creó tablas).
- Comandos y servicios aislados para esta sesión: Compose `crashmemory-v02`, PostgreSQL `54330`, Redis `6390`, MinIO `9011/9012`, API `4311`, web `3001`.
- Criterios de aceptación: aislamiento real de dos usuarios; FKs impiden asociar conexión, blob o evidencia ajena; dinero/fechas conservan semántica; evidencia e históricos son inmutables; login/logout/callbacks seguros; CSRF y origen comprobados; logs redactados.
- Cambios necesarios en README: migración, seed, login, proxy de mismo origen, secretos de sesión/cifrado y comandos de prueba ejecutados.
- Límites del alcance: sin OAuth Gmail, sync, extracción, reconciliación, envíos Telegram ni UI de gestión; sólo base y primitivas reservadas a V02.

## Acta de entrega

- Estado: `lista_para_integrar`.
- Resumen del comportamiento disponible: migración PostgreSQL del modelo MVP; repositorios con pertenencia compuesta; entrada canónica revisión → cuerpo/adjuntos; dinero/fechas exactos; evidencia e históricos inmutables ante actualización; sesiones opacas, scrypt, CSRF/origen, cifrado AES-256-GCM, estado OAuth firmado y nonce consumible; proxy web de mismo origen y logs reducidos/redactados.
- Archivos modificados: `packages/db`, `packages/canonical-model`, `packages/security`, rutas y pruebas de `apps/api`, proxy/enlace de `apps/web`, Compose/env/CI/Turbo, README, contrato de extracción, ADR de privacidad remota y actas factuales V01/I00.
- Commit de entrega: `0a63ba0b3baad477d2b03a2523e059775fd8ea39` (`feat(memory): add secure persistence and auth (V02)`).
- Rama remota y SHA publicados: `origin/codex/v02-memory-security` → `0a63ba0b3baad477d2b03a2523e059775fd8ea39`, verificado con `git ls-remote`.
- Resultado del push de cada commit; cualquier fallo pendiente: primer push exitoso con upstream configurado; sin fallos. Este commit documental registra la entrega publicada y también se pusheará inmediatamente.
- Pruebas ejecutadas, comando y resultado: desde PostgreSQL 17.6 nuevo y volumen vacío, `pnpm install --frozen-lockfile` pasó; `TEST_DATABASE_URL=postgresql://crashmemory:crashmemory@127.0.0.1:54330/crashmemory_v02 pnpm check` pasó con 17 pruebas totales y cero skips, incluidos repositorios/auth reales, rollback de migración, cifrado y sentinel de logs; `pnpm build` pasó; `pnpm audit` informó cero vulnerabilidades conocidas. `pnpm db:migrate` aplicó `0001` y una repetición fue idempotente; `pnpm db:seed` creó usuario sintético. Root ejecutó QA SQL independiente y confirmó PASS para pertenencia/inmutabilidad, parciales, escala >18, `NaN` e infinitos.
- Comprobaciones pendientes y motivo: CI remoto del commit de entrega `0a63ba0` terminó `success` en la ejecución `35550611228`. Sólo queda la validación del resultado combinado que corresponde a I00 antes de integrar.
- Migración y recuperación ante fallo: `0001_v02_memory_security.sql` es transaccional y se serializa con advisory lock; la prueba fuerza una transacción fallida y demuestra que rollback, lock y conexión se recuperan. En local, `docker compose -p crashmemory-v02 -f infra/compose/docker-compose.yml down -v` elimina sólo el volumen aislado; no existe downgrade destructivo para datos reales.
- Instrucciones de uso añadidas al README: instalación congelada, Compose portable y Colima opcional, migración/idempotencia, seed, API/login/logout, CSRF/cookie/origen, proxy web, claves externas, modelo persistente, CI PostgreSQL, checks y límites.
- Limitaciones o riesgos concretos: no hay registro/recuperación de contraseña, OAuth Gmail/callback HTTP, bytes en ObjectStorage, extracción, jobs, reconciliación, Telegram, borrado/exportación ni UI de gestión. V03 implementa ObjectStorage/runtime; V04 consume helpers de sesión/CSRF y nonce OAuth; V05 implementa el ADR remoto sin afirmar ZDR; V09 implementa ciclo de vida.
- Consumo de agente, si el cliente lo informa: no informado.
- Escalaciones de modelo y justificación: ninguna; se usa el modelo asignado.
- Handoff para la siguiente sesión: V03 puede consumir outbox/esquema y añadir ObjectStorage sin cambiar pertenencia. V04 usa `resolveSession`, `verifySessionCsrf`, `OAuthStateSigner` y `OAuthCallbackRepository`; produce `source_revision_bodies/source_attachments`. V05 consume `SourceRepository.getExtractionInput` y `docs/adr/0002-remote-model-privacy.md`.

## Cierre por I00

- Dependencias e interfaces verificadas: pendiente.
- Rama de integración y base de `main`: pendiente.
- Validaciones sobre el resultado combinado: pendiente.
- Verificación de README y cambios de configuración: pendiente.
- Resultado de integración: pendiente.
- SHA integrado: pendiente.
- Push de main y SHA verificado en origin/main: pendiente.
- Estado de CI remoto y checks requeridos: pendiente.
- Estado final y cupo liberado: pendiente.

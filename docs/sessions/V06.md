# V06 · Reconciliación y API de obligaciones

## Encargo

- ID y título: V06 · Reconciliación y API de obligaciones.
- Sesión padre: I00 · Coordinación e integración.
- Modelo exacto y esfuerzo: `gpt-5.6-sol` / `high`, asignados por el plan para identidad y concurrencia.
- Objetivo verificable: convertir candidatos V05 con evidencia en obligaciones versionadas; deduplicar sólo identidad de factura verificable; proteger correcciones; exponer API autorizada y publicar eventos para V07.
- Hito: MVP 1 · V06.
- Dependencias integradas: V01–V05 y V07 en `origin/main` al inicio.
- Commit base de `main` y SHA verificado de `origin/main`: `ac3c0923b6eab40b21f325d6bf778f34ed2f19ca`.
- Rama exclusiva/remota: `codex/v06-reconciliation` / `origin/codex/v06-reconciliation`.
- Worktree exclusivo: `/Users/estebanromero/Documents/ChatGPT/CrashMemory-worktrees/v06-reconciliation`.
- Cupo global: reservado por I00/root dentro del máximo de cuatro.
- Archivos propios: `packages/reconciliation`, migración `0008`, rutas `apps/api/src/obligations.ts`, consumidor en `apps/worker`, pruebas, contrato HTTP, README y esta acta. Extensiones aditivas en V05/V07 y guard de delivery actual.
- Contratos consumidos/producidos: candidatos/evidencia V05, receipt V03 `PoolClient`, eventos `obligation.*.v1`, scheduler/delivery V07 y [HTTP V06](../contracts/reconciliation-http-v1.md).
- Migración reservada: `0008_v06_reconciliation.sql`; `0007_v05_model_budget_periods.sql` llegó por `origin/main` verificado y no se edita en V06.
- Servicios aislados: Compose `crashmemory-v06`, PostgreSQL `54336/crashmemory_v06`, Redis `6396`, MinIO `9021/9022`, API `4316`.
- Criterios de aceptación: identidad fuerte sin fusionar meses por cuenta/título/importe; un ID lógico y revisiones inmutables; replay y dos workers sin duplicados; conflictos y expectedVersion; dos usuarios y CSRF; evidencia autorizada; V07 cancela/reprograma y valida versión antes de HTTP.
- Límites: sólo Gmail, cuerpo y PDF de texto; sin OCR, alta manual general, registro público ni proveedores reales en pruebas.

## Acta de entrega

- Estado: `lista_para_integrar`; I00 integra y comprueba `origin/main`.
- Comportamiento: la extracción propone emisor y número de factura/recibo/liquidación sólo si constan en la cita; el reconciliador vuelve a verificar etiqueta, límites de token y evidencia. Serializa por identidad con advisory lock en el mismo cliente del receipt, vincula cada candidato una vez y conserva una única versión actual. Un evento antiguo deja propuesta `out_of_order` sin alterar vencimiento ni recordatorio vigente. Versiones y correcciones son inmutables. Confirmar/corregir protege campos; la contradicción queda visible y suspende avisos; resolver devuelve al estado previo `candidate` o `confirmed`. Pago/descarte permanecen terminales. API y evidencias son owner scoped; trabajos V05 sin candidato aparecen en `/extraction/reviews`.
- Archivos modificados: paquete `reconciliation`, migración `0008`, extracción, repositorios, contratos, worker, notificaciones, API, pruebas, lockfile, README y contrato HTTP.
- Commit de entrega y SHA remoto: se comunican a I00 tras el push; el acta evita autorreferenciar su propio commit.
- Resultado del push: verificar `origin/codex/v06-reconciliation` antes del handoff; no se acumulan commits locales.
- Pruebas: migración limpia `0001`–`0008`, incluida `0007` de V05, en PostgreSQL 17.6 del namespace V06; `pnpm check` con `TEST_DATABASE_URL`, Redis y MinIO pasó sin servicios omitidos sobre el merge preparado con `origin/main` `efdd79deaa27893cece9f5a7e86d0ab884df5927`; `pnpm build` pasó. Una primera ejecución paralela canceló transitoriamente suites DB/runtime; ambas suites focales y la repetición integral pasaron. Pruebas focales V05/V06, API y V07 pasaron. API real arrancó en `4316`: `/healthz` dio 200, obligaciones/revisiones sin cookie dieron 401 y `no-store`. La prueba V06 cubre candidato V05 real, receipt V03, dos workers, referencias fuertes y débiles, facturas mensuales distintas, corrección/confirmación, conflicto/resolución, replay, versión obsoleta y scheduler V07.
- Migración/recuperación: `0008` es transaccional. El outbox y recibo V03 garantizan que efecto y confirmación de consumo se confirmen juntos; replay salta el efecto completado. No se modifican migraciones V01–V07 aplicadas.
- README: comandos de Compose, migración, API y pruebas V06 comprobados; referencia del contrato HTTP para V08.
- Limitaciones: sin credenciales reales Gmail/Telegram ni envío externo durante V06; validación real queda para V10. Una factura sin ancla explícita sigue separada y necesita revisión; no se adivina identidad. `out_of_order` se puede resolver explícitamente, pero no cambia la versión automáticamente.
- Consumo de agente: no informado por el cliente.
- Escalaciones de modelo: ninguna.
- Handoff V08: consumir [rutas HTTP V06](../contracts/reconciliation-http-v1.md), mostrar `protectedFields`, `conflicts` y `/extraction/reviews`; conservar `expectedVersion` de detalle en cada mutación. Las rutas de fuente/callback Gmail y vínculo Telegram mantienen sus contratos anteriores.
- Handoff V09: los vínculos `reconciliation_candidate_links` y `obligation_conflicts` dependen por FK de usuario, fuente y obligación; borrar fuente debe reevaluar soporte antes de borrar una obligación confirmada y emitir cancelación de avisos.

## Cierre por I00

- Dependencias e interfaces verificadas:
- Rama de integración y base de `main`:
- Validaciones sobre resultado combinado:
- Verificación de README y migraciones:
- SHA integrado, push de `main` y CI:
- Estado final y cupo liberado:

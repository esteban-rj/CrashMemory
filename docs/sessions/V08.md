# V08 · Web mínima

## Encargo

- ID y título: V08 · Web mínima.
- Sesión padre: I00.
- Modelo exacto y esfuerzo: `gpt-5.6-luna` / `medium`.
- Objetivo verificable: web responsive en español sobre las APIs autenticadas de Gmail, obligaciones, evidencia, revisiones de extracción y Telegram.
- Hito: MVP 1 · V08.
- Dependencias integradas y commits: V04, V06 y V07 en `origin/main`.
- Commit base de `main`: `3075b6edc5b38630fc0c3c0edd0eb0ae7491b5fd`.
- SHA verificado de `origin/main`: `3075b6edc5b38630fc0c3c0edd0eb0ae7491b5fd` al inicio.
- Rama exclusiva: `codex/v08-minimal-web`.
- Rama remota/upstream: `origin/codex/v08-minimal-web`.
- Worktree exclusivo: `/Users/estebanromero/Documents/ChatGPT/CrashMemory-worktrees/v08-minimal-web`.
- Cupo global asignado: reservado por I00.
- Archivos/paquetes propios: `apps/web`, README y esta acta.
- Contratos consumidos: login/logout y CSRF; Gmail; reconciliación HTTP V06; evidencia; revisiones; Telegram y recordatorios.
- Migraciones previstas: ninguna.
- Comandos y servicios aislados: web `3008`, API `4318`; PostgreSQL/Redis/MinIO usan el namespace de V08 cuando I00 ejecute QA integrado.
- Criterios de aceptación: sesión real con cookie y expiración 401; controles de propietario delegados a API; mutaciones con CSRF y `expectedVersion`; conflictos refrescados en 409; vista responsive y accesible; no exponer secretos ni seguir enlaces de correo.
- Límites: lifecycle/desconexión, exportación y borrado son V09; no se incluye dashboard ni chat.

## Acta de entrega

- Estado: `integrada`.
- Resumen: login/logout, navegación de obligaciones y conexiones, estado Gmail/Telegram, detalle con historial/evidencia textual, revisiones, paginación por cursor, avisos e intentos (`sent`/`failed`/`unknown`), descarga de fuente autenticada, acciones de confirmar/corregir/pagar/descartar y resolver conflictos. Los controles de desconexión Gmail/desvinculación Telegram llaman el contrato V09 y muestran “Acción no disponible todavía.” cuando la API responde 404.
- Archivos modificados: `apps/web/app/page.tsx`, `apps/web/app/layout.tsx`, `apps/web/app/styles.css`, `apps/web/tsconfig.json`, `README.md` y esta acta.
- Commit de producto integrado: `bbc28e5e38570727c0e15eda617afc6d50bfa204`.
- CI del producto: ejecución `35755880950`, `SUCCESS`.
- Pruebas ejecutadas: `pnpm --filter @crashmemory/web typecheck`, `pnpm --filter @crashmemory/web build` y Prettier pasan. En el namespace `crashmemory-v08` se levantaron PostgreSQL `54338`, Redis `6398` y MinIO `9025/9026`; se aplicaron las ocho migraciones, se creó `v08-user@example.test` y `TEST_DATABASE_URL=postgresql://...:54338/... pnpm --filter @crashmemory/api test` pasó 6/6 (auth, Gmail callback, obligaciones/evidencia/409, Telegram/reminders y demo). Con API `4318` y web `3008`, `/Users/estebanromero/.npm/_npx/6de2aa2fded2970c/node_modules/.bin/agent-browser` comprobó login real, lista vacía, conexiones, avisos vacíos, recarga con sesión persistida, logout y vista móvil 390x844 sin errores de página; una obligación fixture sintética se visualizó en el listado.
- QA web funcional completado con API `4318`, web `3008`, PostgreSQL `54338`, Redis `6398` y MinIO `9025/9026`, usando el usuario sintético `v08-user@example.test` y una contraseña suministrada sólo mediante variable de entorno. Se visualizaron dos obligaciones con la misma evidencia autenticada; se abrió detalle, historial, cita, texto y descarga `/source`; se corrigió sólo el título de B y se conservaron `COP 96.25` y `2026-11-20`; B pasó a pagada; se rechazó el conflicto de B y se aceptó el conflicto de A; una mutación con `expectedVersion: 1` obsoleta respondió `409 stale_version`; Avisos mostró un recordatorio `resolved` e intento `sent`; recarga conservó sesión y logout la limpió; revocar sesiones en DB produjo pantalla de login por `401`; viewport móvil 390×844 no mostró errores de página. No se usaron correos ni proveedores reales.
- Comprobaciones pendientes: ninguna de V08. V09 aún debe integrar sus rutas lifecycle para que los botones de desconexión/desvinculación dejen de mostrar “Acción no disponible todavía.”.
- Migración y recuperación: no aplica. La UI refresca detalle y lista tras mutaciones; ante 401 limpia sesión y ante 409 vuelve a consultar la versión vigente.
- Instrucciones añadidas al README: arranque en puerto 3008, rewrite API, flujo de login, callback Gmail y límite V09.
- Limitaciones: el entorno de demo sin persistencia responde 503 al login; las rutas lifecycle pueden responder 404 hasta integrar V09 y la UI informa “Acción no disponible todavía.”. Los avisos reales dependen de la política automática de V07. Los fixtures QA fueron sintéticos, creados con `SourceRepository`, `ReminderRepository`, `S3ObjectStorage` y SQL de reconciliación sobre el namespace aislado; sus identificadores y contraseña quedaron fuera de Git.
- Regresión de concurrencia: se retrasó artificialmente 1.5 s la lectura de `response.json()` del detalle, se cerró la sesión antes de recibirla y la pantalla permaneció en login sin repoblar detalle ni mensaje; también se cubre fallo de red de generaciones viejas. La generación de sesión invalida respuestas y `401` obsoletos al cambiar de usuario. Las fechas `instant` se formatean con su `due.timeZone` explícita.
- Escalaciones de modelo: ninguna.
- Integración: `origin/main` fue actualizado por avance rápido a `bbc28e5e38570727c0e15eda617afc6d50bfa204`; no se hizo force-push ni se modificó el historial.
- Handoff: I00 conserva el bloqueo serial de integración; V08 queda disponible en `origin/main` para el recorrido combinado con V09.

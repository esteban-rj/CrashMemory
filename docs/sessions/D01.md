# D01 — Arranque completo en Docker

- Encargo: completar el paso de uso en Docker señalado por el usuario.
- Rama: `codex/d01-docker-startup`.
- Worktree: `d01-docker-startup`, independiente de `main`.
- Alcance: imagen Node fijada; Compose de migración, seed, bucket, API, worker, scheduler y web; API escucha en la interfaz del contenedor sólo en este modo; README y variables de ejemplo.
- Datos de validación: namespace `crashmemory-d01`, PostgreSQL `54344`, Redis `6404`, MinIO `9038/9039`, API `4324`, web `3014`; cuenta y claves sintéticas privadas fuera de Git.
- Verificación: Compose config; build; comando documentado `up -d --build --wait` exitoso; PostgreSQL, Redis, MinIO, API y web sanos; migración, seed y bucket terminaron con éxito; worker y scheduler en ejecución; HTTP de web/API `200`; login a través del rewrite web `200` y cookie presente. `down` terminó y conservó los tres volúmenes. Checks de estructura, formato, lint, tipos y secretos aprobados. Se comprobó sin Gmail, Telegram ni modelo remotos.
- Persistencia: el journal es un directorio privado del host montado en los contenedores; `down` conserva journal y volúmenes.
- Estado: listo para integrar tras la validación final y publicación de la rama. El SHA y la CI se comunicarán después del push.

- CI candidata inicial: GitHub rechazó la descarga de la imagen antigua desde Quay antes de las pruebas, incluso al reintentar. Se cambió Compose y CI a una imagen MinIO de GHCR construida desde el código oficial, fijada por digest y probada localmente en arm64.

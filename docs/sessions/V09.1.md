# Sesión V09.1 — Recuperación y cierre de V09

## Encargo

- ID y título: V09.1 · Backup/restore y cierre de ciclo de vida MVP 1.
- Sesión padre: V09; coordinación I00.
- Modelo exacto y esfuerzo: `gpt-5.6-sol` / `high`.
- Objetivo verificable: completar V09 con restore offline probado de PostgreSQL y MinIO, journal externo que evite resurrección, reintento de limpieza de objetos y QA de borrado multisoporte, propietario y concurrencia.
- Hito: MVP 1 Gmail → obligaciones con evidencia → Telegram.
- Dependencias integradas y commits: V04/V06/V07 en `origin/main` `3075b6edc5b38630fc0c3c0edd0eb0ae7491b5fd`; candidato V09 padre `b4c7d9ae150b7e1b952ce341517202fcd264d547` publicado en `origin/codex/v09-lifecycle`.
- Commit base de `main`: `3075b6edc5b38630fc0c3c0edd0eb0ae7491b5fd`.
- SHA verificado de `origin/main` al inicio: `3075b6edc5b38630fc0c3c0edd0eb0ae7491b5fd`.
- Rama exclusiva: `codex/v09-lifecycle-recovery`.
- Rama remota/upstream: `origin/codex/v09-lifecycle-recovery` al publicar la entrega.
- Worktree exclusivo: `/Users/estebanromero/Documents/ChatGPT/CrashMemory-worktrees/v09-lifecycle-recovery`.
- Cupo global asignado: tercero durante I00 + V08 + V09.1; máximo observado tres.
- Archivos/paquetes propios: `packages/lifecycle`, migración 0009 aún no integrada, guardias de reconciliación/notificaciones/Gmail, rutas API lifecycle, README y actas.
- Contratos consumidos y producidos: endpoints V09 del padre, journal AES-256-GCM por línea con identidad y fsync, archivo cifrado PostgreSQL+objetos, CLI offline `backup`, `restore`, `cleanup:retry`.
- Migraciones previstas y orden reservado: completa `0009_v09_lifecycle.sql` antes de su primera integración a main; añade alcance `gmail_disconnect`.
- Comandos y servicios aislados: Compose `crashmemory-v09-recovery`, PostgreSQL `54340` (base de fuente y bases de restore separadas), Redis `6399`, MinIO `9027/9028`; bucket distinto por prueba.
- Criterios de aceptación: borrado sin resurrección, correo y otra obligación preservados cuando solo se borra un conocimiento, cambio de política sin envío de job encolado, backup/restore real sin desactivar FKs, journal actual validado antes de mutar, comando de limpieza idempotente.
- Cambios necesarios en README: rutas, variables, parada y drenaje, backup y restore con binarios nativos o contenedor, journal externo, `pendingObjectCleanup`, límites y QA reproducible.
- Límites del alcance: fixtures sintéticos; no se invocan Gmail ni Telegram reales, ni se añaden pantallas de V08.

## Justificación de la excepción

V09 Terra/high dejó checkpoints remotos pero terminó repetidamente con QA de restore pendiente. La última continuación volvió a terminar sin haberlo probado. I00 autorizó V09.1 Sol/high como decimotercera unidad, sin nueva entrega funcional. La sesión parte de main verificado y avanzó por fast-forward al candidato padre sin tocar el diff no publicado del worktree V09; el cambio útil de validación/replay de journal se portó y amplió aquí. La rama y el worktree son exclusivos. La integración a main continúa bajo el bloqueo serial de I00.

## Acta de entrega

- Estado: `lista_para_integrar` tras validación y publicación de la rama combinada con V08; la integración a main corresponde a I00.
- Resumen del comportamiento disponible: restore offline autentica el journal completo y su identidad antes de tocar destino; exige base sin esquema y bucket vacío; comprueba hashes de dump y objetos; restaura esquema/datos/constraints con `pg_restore --single-transaction --exit-on-error`, reaplica tombstones y ejecuta borrados según su intención original. La desconexión Gmail también queda en journal. Borrados de conocimiento bloquean candidatos tardíos sin borrar correos que aún sustentan otra obligación. La limpieza MinIO pendiente se puede reintentar tras borrar usuario/fuente, con guardia contra blobs vivos.
- Archivos modificados: consultar el commit de entrega de esta rama; incluye `AGENTS.md`, plan, README, `packages/lifecycle`, `packages/notifications`, `packages/reconciliation`, `packages/gmail` y `packages/runtime`.
- Commit de entrega y SHA remoto: registrar mediante `git rev-parse origin/codex/v09-lifecycle-recovery` tras push; SHA exacto comunicado a I00 fuera del acta para evitar un commit recursivo.
- Resultado del push de cada commit: primer commit V09.1 `9845c1c3ab8128a30e2f9ed4eea03e655764692c` publicado y verificado en `origin/codex/v09-lifecycle-recovery`; el SHA final tras incorporar `origin/main` se consulta en esa rama remota. No hay commits nuevos de V09.1 sin push.
- Pruebas ejecutadas: restore PostgreSQL 17.6 + MinIO reales en namespace V09.1 con bases fuente/destino y buckets separados: 1/1, cero skips; probó copia cifrada, borrados posteriores, otro propietario, otra obligación con mismo correo, candidato pendiente, objeto compartido, Telegram/Gmail, journal errado/parcial/desconocido y archivo alterado. Log local `/private/tmp/crashmemory-v09-recovery-restore.log`. Los comandos de CLI documentados también se probaron con fuentes sintéticas: `backup_completed objectCount=1` y `restore_completed restoredObjects=1 replayedDeletes=3`. `packages/lifecycle/test/lifecycle.test.ts` 5/5 (incluye dos instancias inicializando/escribiendo ocho entradas a la vez, bloqueo concurrente y reintento de MinIO); `packages/notifications/test/notifications.test.ts` 4/4 (incluye política falsa con evento encolado y backfill >100). `pnpm check` final 24/24 tareas y build 15/15; logs `/private/tmp/crashmemory-v09-recovery-check-final.log` y `/private/tmp/crashmemory-v09-recovery-build-final.log`. El check general tiene un único skip intencional de restore porque no se proporciona `TEST_RESTORE_DATABASE_URL`/`TEST_PG_CONTAINER`; el comando real separado sí lo ejecutó sin skips.
- Validación combinada con V08: `origin/main` `2943ecc6999bb1e79f876ee3a922af0b081d3e43` incorporado en merge preparado; `pnpm check` 24/24 y `pnpm build` 15/15 antes del commit, con logs `/private/tmp/crashmemory-v09-recovery-combined-check.log` y `/private/tmp/crashmemory-v09-recovery-combined-build.log`. Único skip: restore real en suite general, probado aparte 1/1 con PG17/MinIO.
- Comprobaciones pendientes y motivo: CI remoto del SHA final y revisión/integración serial por I00.
- Migración y recuperación ante fallo: el journal actual nunca se empaqueta como autoridad en el snapshot; el archive conserva hash del prefijo del journal y los datos de tombstone no se restauran desde PostgreSQL. Si restore falla tras escribir algún objeto o reaplicar barreras, descartar la base/bucket de destino y repetir sobre destinos vacíos sin arrancar API, worker o scheduler.
- Instrucciones de uso añadidas al README: comandos reales offline y `cleanup:retry`.
- Limitaciones o riesgos concretos: los flujos reales Google/Telegram y una restauración de gran volumen no están validados; el archivo se construye en memoria y requiere espacio acorde al dump y originales. La etiqueta `LIFECYCLE_QUIESCED=true` declara una parada operativa; el programa no puede verificar procesos externos por sí mismo.
- Consumo de agente: el cliente no informa una cifra de tokens fiable para esta sesión.
- Escalaciones de modelo y justificación: V09 Terra/high → V09.1 Sol/high por restore y concurrencia pendientes tras varios intentos, documentado arriba.
- Handoff para I00: revisar commit remoto, ejecutar checks sobre combinación con V08 y avanzar main solo bajo su bloqueo serial.

## Cierre por I00

- Dependencias e interfaces verificadas: pendiente.
- Rama de integración y base de `main`: pendiente.
- Validaciones sobre el resultado combinado: pendiente.
- Verificación de README y cambios de configuración: pendiente.
- Resultado de integración: pendiente.
- Push de main y SHA verificado en origin/main: pendiente.
- Estado de CI remoto y checks requeridos: pendiente.
- Estado final y cupo liberado: pendiente.

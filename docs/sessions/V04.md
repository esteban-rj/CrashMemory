# Sesión V04 — Gmail

## Encargo

- ID y título: V04 · Gmail.
- Sesión padre: I00.
- Modelo exacto y esfuerzo: `gpt-5.6-terra` / `high`.
- Objetivo verificable: conectar Gmail con `gmail.readonly`, sincronizar histórico acotado e incremental recuperable, y producir revisiones canónicas consumibles por V05.
- Hito: MVP 1, V04.
- Dependencias integradas y commits: V01–V03, base `c098b6dc753406e9e8d8f05dcef5c9868ec31766`.
- Commit base de `main`: `c098b6dc753406e9e8d8f05dcef5c9868ec31766`.
- SHA verificado de `origin/main`: `c098b6dc753406e9e8d8f05dcef5c9868ec31766`.
- Rama exclusiva: `codex/v04-gmail`.
- Rama remota/upstream: pendiente de publicación.
- Worktree exclusivo: `/Users/estebanromero/Documents/ChatGPT/CrashMemory-worktrees/v04-gmail`.
- Cupo global asignado (1–4): 3 de 4 (root=1, I00=2, V04=3, V05=4).
- Archivos/paquetes propios: `packages/gmail`, rutas Gmail de API, migración V04, documentación y pruebas V04.
- Contratos consumidos y producidos: `extraction-input-v1`, `events-v1`, `http-v1`; produce revisiones/cuerpos/adjuntos y `source.item.revision.created.v1` para V05.
- Migraciones previstas y orden reservado: `0004_v04_gmail_sync.sql` (`0003_v05_model_budget` está reservado por V05).
- Comandos y servicios aislados para esta sesión: `pnpm --filter @crashmemory/gmail test`, puertos y Compose V03 hasta actualizar la guía V04.
- Criterios de aceptación: páginas persistidas antes del cursor; replays no duplican; `historyId` vencido exige resync; watch renovable y polling de respaldo; sólo `gmail.readonly`.
- Cambios necesarios en README: configuración OAuth, Pub/Sub/webhook, sync, recuperación y límites.
- Límites del alcance: no OCR, extracción, conciliación, Calendar ni scopes Gmail de modificación.

## Acta de entrega

- Estado: `lista_para_integrar`.
- Resumen del comportamiento disponible: OAuth `gmail.readonly` con estado/nonce/sesión/CSRF, MIME de cuerpo y PDF, persistencia canónica para V05, runner con refresh/polling/resync/watch y webhook Pub/Sub OIDC durable.
- Archivos modificados: `packages/gmail`, repositorios y migración V04 de DB, API/scheduler, `.env.example`, `README.md`, actas y pruebas.
- Commit de entrega: `89cd001ed109de31c451381b4f8fbe7af195bea1` (`feat(gmail): add recoverable Gmail sync (V04)`).
- Rama remota y SHA publicados: `origin/codex/v04-gmail` en `89cd001ed109de31c451381b4f8fbe7af195bea1`.
- Resultado del push de cada commit; cualquier fallo pendiente: entrega publicada con `git push -u origin HEAD`; sin fallo pendiente.
- Pruebas ejecutadas, comando y resultado: `pnpm --filter @crashmemory/gmail typecheck`, `pnpm --filter @crashmemory/api typecheck`, `pnpm --filter @crashmemory/scheduler typecheck`, `pnpm --filter @crashmemory/db typecheck`; con `TEST_DATABASE_URL=...:54332/crashmemory_v04`, pruebas Gmail (6), API (4) y DB (2), todos `pass`; `pnpm lint`, `pnpm format:check`, `pnpm check:secrets` y `git diff --check`, correctos.
- Comprobaciones pendientes y motivo: validación e integración serial por I00.
- Migración y recuperación ante fallo: `0004_v04_gmail_sync.sql` es transaccional; cursor sólo confirma páginas completas; un 404 de history activa resync acotado y un `invalid_grant` exige reautorización.
- Instrucciones de uso añadidas al README: configuración OAuth/Pub/Sub, runner, límites de sync y comandos de verificación V04.
- Limitaciones o riesgos concretos: OAuth y Pub/Sub reales requieren credenciales, tema, service account y URL HTTPS configurados fuera de Git; V05 aún no consume los eventos.
- Consumo de agente, si el cliente lo informa: no informado.
- Escalaciones de modelo y justificación: ninguna.
- Handoff para la siguiente sesión: V05 consume `SourceRepository.getExtractionInput` después de que V04 persista la revisión completa y emita outbox.

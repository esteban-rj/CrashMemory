# V01 — Foundation: contratos y bootstrap local

## Encargo

- ID y título: V01 · Foundation: contratos y bootstrap local.
- Sesión padre, si existe: I00 · Coordinación e integración.
- Modelo exacto y esfuerzo: `gpt-5.6-terra` / `high`.
- Objetivo verificable: dejar contratos versionados y un monorepo que pueda ejecutar una demo local sin credenciales de proveedores.
- Hito: Base V01–V03.
- Dependencias integradas y commits: plan v1.1, `85aadb5`.
- Commit base de `main`: `85aadb5609e9b57d753d9936c883b101fcb4f605`.
- SHA verificado de `origin/main`: `85aadb5609e9b57d753d9936c883b101fcb4f605` (asignado por I00; la consulta remota inicial no pudo resolver `github.com`).
- Rama exclusiva: `codex/v01-foundation`.
- Rama remota/upstream: `origin/codex/v01-foundation`.
- Worktree exclusivo: `/Users/estebanromero/Documents/ChatGPT/CrashMemory-worktrees/v01-foundation`.
- Cupo global asignado (1–4): 3 de 4 (raíz=1, I00=2, V01=3).
- Archivos/paquetes propios: raíz del monorepo, `apps/*`, `packages/contracts`, `docs/contracts`, `docs/adr`, `infra`, `.github`, fixtures y este acta.
- Contratos consumidos y producidos: consume plan/revisión; produce contratos HTTP, canónicos, eventos/outbox, modelos de datos, privacidad IA y demo.
- Migraciones previstas y orden reservado: no hay migraciones en V01; V02 reserva el primer esquema y migraciones nuevas, sin editar una migración aplicada.
- Comandos y servicios aislados para esta sesión: `pnpm demo`, puertos API `4310`, web `3000`; Compose usa `COMPOSE_PROJECT_NAME=crashmemory-v01` y puertos `54329`, `6389`, `9009/9010`.
- Criterios de aceptación: demo local sin credenciales cloud; contratos de login, identidad/versionado, dinero/fechas, evidencia, eventos y outbox definidos; CI verifica README por entrega; no se crean módulos diferidos.
- Cambios necesarios en README: instalación, demo, servicios opcionales locales, variables, checks y límites comprobables.
- Límites del alcance: no implementa autenticación, OAuth, persistencia, extracción, sincronización ni envío a Telegram.

## Acta de entrega

- Estado: `lista_para_integrar`.
- Resumen del comportamiento disponible: monorepo pnpm/Turborepo con API Fastify demo, esqueleto Next.js, puntos de entrada worker/scheduler, schemas Zod de contratos y servicios Compose locales aislados. La demo devuelve sólo una obligación sintética en memoria.
- Archivos modificados: raíz de workspace/CI/checks; `apps/api`, `apps/web`, `apps/worker`, `apps/scheduler`; `packages/contracts`; `docs/contracts`, `docs/adr`; `infra/compose`, fixtures sintéticas, README y esta acta.
- Commit de entrega: `c884e6e2b5a606af107a10e196f8329dc5955df6` (`feat(foundation): add V01 contracts and local demo`).
- Rama remota y SHA publicados: `origin/codex/v01-foundation` → `c884e6e2b5a606af107a10e196f8329dc5955df6`, verificado con `git ls-remote`.
- Resultado del push de cada commit; cualquier fallo pendiente: push inicial exitoso con upstream configurado; sin fallos pendientes. Este commit documental corrige el acta posterior a esa verificación.
- Pruebas ejecutadas, comando y resultado: `pnpm check` pasó (estructura, ESLint, Prettier, tipos, 4 tests de contrato/API y 2 tests del scanner); `pnpm build` pasó e incluyó `next build` de Next.js 16.3.5; `pnpm audit` terminó sin vulnerabilidades conocidas. `API_PORT=44310 pnpm demo` y curls a `/healthz` y `/api/v1/demo/obligations` devolvieron la demo sintética esperada. `docker --context colima-crashmemory compose --env-file .env.example -f infra/compose/docker-compose.yml up -d --force-recreate --wait` dejó PostgreSQL, Redis y MinIO `healthy` en loopback; se limpió después con `down -v`.
- Comprobaciones pendientes y motivo: ninguna técnica; QA browser final de raíz pasó con web `:3301` → click → API `:44310/api/v1/demo/obligations`, JSON `candidate` con `48250.00` COP y `synthetic-demo`, sin errores JavaScript.
- Migración y recuperación ante fallo: no aplica aún; V02 creará migraciones inmutables.
- Instrucciones de uso añadidas al README: instalación con lockfile, demo/curls, web con puertos y `API_BASE_URL` aislados, Compose portable y uso opcional de contexto Colima, checks y límites.
- Limitaciones o riesgos concretos: demo sintética en memoria; no representa servicios externos, persistencia, login, OAuth ni envío Telegram. Los esqueletos worker/scheduler no ejecutan jobs; V03/V07 implementarán esos comportamientos.
- Consumo de agente, si el cliente lo informa: no informado.
- Escalaciones de modelo y justificación: ninguna.
- Handoff para la siguiente sesión: V02 implementa el esquema/migraciones y autenticación conforme a `docs/contracts`; debe preservar evidencia inmutable, dinero decimal, fecha civil y separación de sesión local/OAuth Gmail.

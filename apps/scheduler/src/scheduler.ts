import { CONTRACT_VERSION } from "@crashmemory/contracts";

console.log(
  `CrashMemory scheduler skeleton ready for reminder policies (${CONTRACT_VERSION}).`,
);
console.log("V07 adds notification scheduling and delivery state transitions.");

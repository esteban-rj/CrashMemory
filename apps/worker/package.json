{
  "name": "@crashmemory/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/worker.ts",
    "acceptance:seed": "node --import tsx src/acceptance-fixture.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/**/*.test.ts"
  },
  "dependencies": {
    "@crashmemory/contracts": "workspace:*",
    "@crashmemory/db": "workspace:*",
    "@crashmemory/extraction": "workspace:*",
    "@crashmemory/gmail": "workspace:*",
    "@crashmemory/model-gateway": "workspace:*",
    "@crashmemory/notifications": "workspace:*",
    "@crashmemory/reconciliation": "workspace:*",
    "@crashmemory/runtime": "workspace:*",
    "@crashmemory/security": "workspace:*"
  }
}
